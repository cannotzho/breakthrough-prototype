import { CombinationRecipe } from '../combat/types';

export const COMBINATIONS: CombinationRecipe[] = [
  // Recipes are defined as { ingredients: [cardIdA, cardIdB], result: CardDefinition }
  // The combat engine picks up new recipes automatically.
];
